# Лабораторная работа (ЛР) 12. Сборка


**Студенту на пару:** работать по [методичке](/labs/12/method) — шаги, Git и чеклист сдачи. Эту карточку читать, если нужна теория простыми словами.

4 ч. Тема лекций 14 и 17. Лекцию слушать не обязательно. После ЛР11. Предзащита — следующая работа, не сегодня.


## Зачем эта работа

Куски есть: красивый Vue, живой Swagger, строки в pgAdmin. Часто это **три разные** заявки. Сегодня нужна **одна**. Человек нажал «Создать» — тот же номер в Network — та же строка в таблице — после рестарта API она же.

Мок из ЛР6 выключить. Если UI не успели связать, показать один Swagger работу **не** закрывает: соседу всё равно нужен экран.

Найденные дыры запишите списком. Чинить всё подряд сегодня не надо: на ЛР13 будет предзащита и точечный ремонт.

## Цель

Один `id` через интерфейс, запрос и базу. README, по которому сосед поднимает стенд.

## Что сдать

Сценарий (имена ролей свои):

1. Кто принимает входит и создаёт объект **на экране**, не в Swagger.
2. Назначает исполнителя.
3. Исполнитель (второй вход) ставит `InProgress`.
4. Тот же `id` виден во вкладке Network и в `SELECT`.
5. API выключили и включили — запись на месте.

README с нуля: база, миграции, `dotnet run`, `npm run dev`, порты, учебные логины, как проверить один сценарий.

Таблица трёх критериев ЛР1 в `/docs`: шаг, что ждали, куда смотреть (экран / запрос / SQL).

Список известных дефектов — тоже в `/docs`.

---

## Теория на пару

30–40 минут. Больше времени на запуск у двоих.

### Один путь, не три цирка

Плохая защита: «вот список в Vue (мок), вот POST в Swagger (другая заявка), вот SELECT старого seed». Комиссия не может сверить.

Хорошая: записываете на бумаге `id = 17` и везде тыкаете 17.

Клиент слушает порт **5173**, сервер **5000**. В `.env` ровно тот адрес, что в README. Мок выключен одним флагом.

### Почему вдруг «CORS error»

Браузер считает, что сайт на порту 5173 и сайт на порту 5000 — **разные** места (другой порт = другой origin). По умолчанию он не отдаёт ответ JavaScript с чужого порта. Сервер должен явно сказать: «ответы можно читать сайту `http://localhost:5173`».

Как понять, кто виноват:

- в Network запрос красный, статус вроде «CORS» / нет кода ответа — заблокировал **браузер**. API при этом может быть жив: откройте тот же URL в Swagger.
- код 500 — виноват сервер, CORS тут ни при чём.

Разрешайте конкретный адрес, не звёздочку `*` на всё интернет.

### Как читать Network

F12 → вкладка Сеть / Network. Нажимаете «Создать» — появляется строка `tickets` (или ваш путь). Смотрите:

- метод (`POST`);
- код (`201`);
- тело запроса (ушла ли тема);
- тело ответа (какой `id` вернули).

Пустой список — `200` и `[]`. Не ищите JSON в ответе `204` (у нас его нет).

Если экран молчит: сначала Network, потом консоль браузера, потом логи `dotnet`, потом таблица. Не наоборот и не «перепишу всё».

README пишут так, будто автор завтра заболел. Сосед не должен звонить и спрашивать «а пароль какой». Учебный логин — да. Боевой пароль PostgreSQL — нет, только «задайте свой, вот пример без секрета».

### Каркас README (скопировать и заполнить)

```markdown
# … тема …

## Стек
Node …, .NET …, PostgreSQL …

## Подготовка
1. Создать БД …
2. cd backend && dotnet ef database update
3. Задать строку подключения через user-secrets / env (не коммитить)

## Запуск
cd backend && dotnet run          # http://localhost:5000/swagger
cd frontend && npm install && npm run dev   # http://localhost:5173

## Учебные логины
dispatcher / … ; engineer / …

## Проверка
Создать заявку → увидеть id в UI, Network и SELECT в БД.
```

Поставка = этот локальный контур (лекция 17), не облако.

## Порядок на 4 часа

1. `USE_MOCK = false`. `VITE_API_BASE_URL=http://localhost:5000`. Перезапустить Vite.
2. На сервере разрешить origin `http://localhost:5173`.
3. Прогнать сценарий. `id` записать в отчёт.
4. В psql/pgAdmin: `SELECT * FROM tickets WHERE id = …;`
5. Рестарт API, снова GET или обновление списка.
6. Таблица трёх критериев.
7. Переписать README. Отдать ноутбук соседу: он запускает, вы молчите. Где застрял — правите текст, не киваете руками.
8. `.gitignore`: `bin`, `obj`, `node_modules`, `.env` с паролем, дампы.
9. Дефекты списком: «кнопка Назначить на списке неактивна, обход через Swagger» — честно.

## Сдача

Сосед запустил по тексту. Один id везде. На защите этот путь за три минуты. Если сосед не запустил — сдаёте README, не «у меня работало».

## Частые ошибки

- Мок забыли выключить — в Network нет запроса на 5000, в базе пусто.
- CORS `*`, а cookie-вход потом не работает. Лучше сразу конкретный origin.
- В README «просто откройте sln» без `database update`.
- Три критерия скопировали из ЛР1 без «где смотреть».
- Чинют цвет кнопок, а назначение не связано с экраном.
